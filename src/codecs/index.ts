/**
 * Codec registration. Importing this module triggers the side-effect
 * of registering every per-packet fanta codec with the walker. Imported
 * once from `../encode`/`../decode` so users don't have to remember.
 */

import "./ARUP";
